#include <iostream>
#include "list.h"
#include "node.h"

using namespace std;

int main() {
    Node *n1 = new Node(1);
    n1->print();
    //list *L = new list();
    //list *L1 = new list(n1);


    return 0;
}